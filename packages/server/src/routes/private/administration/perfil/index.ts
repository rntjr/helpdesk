import express from 'express'

const app = express.Router()

app.get('/:id', async () => {
  return null
})

app.post('/', async () => {
  return null
})

app.put('/:id', async () => {
  return null
})

app.delete('/:id', async () => {
  return null
})

app.get('/:id/permissao', async () => {
  return null
})

app.post('/:id/perfis', async () => {
  return null
})

app.delete('/:id/permissao/:idPermissao', async () => {
  return null
})

export default app
